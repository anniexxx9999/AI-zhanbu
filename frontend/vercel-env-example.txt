# Vercel 环境变量配置示例
# 在 Vercel Dashboard -> Project Settings -> Environment Variables 中设置

# 后端 API 地址（部署后更新）
NEXT_PUBLIC_API_BASE_URL=https://your-backend-project.vercel.app

# 可选：其他配置
NEXT_PUBLIC_APP_NAME=AstroSoul
NEXT_PUBLIC_APP_VERSION=1.0.0
